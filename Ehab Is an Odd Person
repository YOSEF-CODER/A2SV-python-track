n = int(input())
ans = list(map(int,input().split()))
evenCount=0
for i in ans:
    if i%2==0:
        evenCount+=1
if evenCount==len(ans) or evenCount==0:
    print(*ans)
else:
    ans.sort()
    print(*ans)
